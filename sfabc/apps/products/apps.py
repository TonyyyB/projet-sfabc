from django.apps import AppConfig


class ProductsConfig(AppConfig):
    """Configuration Django de l'app products."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.products'
