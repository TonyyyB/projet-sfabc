from django.apps import AppConfig


class ReviewsConfig(AppConfig):
    """TODO: Vérifier — Configuration Django de l'app reviews."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.reviews'
